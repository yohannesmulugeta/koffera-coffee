# Compiled Pages Publication

Generate the production Vite output on `pages-compiled` so the repository's branch-based GitHub Pages source serves compiled HTML, JavaScript, CSS and public assets instead of development source files.
