# Direct Trusted Pages Deployment

Build the current `main` branch, dispatch `deploy.yml` on the trusted default branch, report the exact production run ID, watch it to completion, and verify the public homepage and compiled assets.
